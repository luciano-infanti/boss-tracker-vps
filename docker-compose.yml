services:
  db:
    image: postgres:16-alpine
    container_name: boss-tracker-db
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: boss_tracker
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
